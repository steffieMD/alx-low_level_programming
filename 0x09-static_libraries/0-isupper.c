#include "main.h"

/**
 * _isupper - checks whether uppercase or not
 * @c: string
 * Return: 0 or 1
 */

int _isupper(int c)
{
	int i;

	if (i >= 'A' && i <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
