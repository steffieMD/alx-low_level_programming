#include "main.h"

/**
 * _islower - Entry point
 *
 * Return: Always 0
 */

int _islower(int c)
{
	char s;

	if (_islower(s))
	{
		_putchar('1' + s);
	}
	else
	{
		_putchar('0' + s);
	}
	
	_putchar('\n');
	return (0);
}
