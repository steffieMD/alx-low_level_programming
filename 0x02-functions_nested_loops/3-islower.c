#include "main.h"

/**
 * _islower - Entry point
 *
 * Return: Always 0
 */

int _islower(int c)
{
	char c;

	if (_islower(c))
	{
		_putchar('1' + c);
	}
	else
	{
		_putchar('0' + c);
	}
	
	_putchar('\n');
	return (0);
}
