#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - determines string length
 * @s: char
 *
 * Return: Always 0
 */

int _strlen(char *s)
{
	int len = _strlen(s);

	return (len);
}
