#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen_recursion - returns the length of a string
 * @s: a string
 * Return: Always 0
 */

int _strlen_recursion(char *s)
{
	int *len;

	len = strlen(s);

	return (len);
}
