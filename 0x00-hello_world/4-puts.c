#include <stdio.h>
	/**
	 * This is the preferred style for multi-line
	 * Puts(prints out).
	 * Return statement.
	 */
int main(void) 
{
	/* Prints something */
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
