#include <stdio.h>
	/**
	 * Main function to return a string.
	 * Puts(prints out).
	 * Return statement.
	 */
int main(void) 
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
